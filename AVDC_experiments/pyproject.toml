[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "avdc-experiments"
version = "0.1.0"
description = "AVDC experiment utilities and policies."
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
include = ["AVDC_experiments*"]

